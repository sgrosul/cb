{
  "name": "cicd-through-ai-lab",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Educational repository focused on CI/CD troubleshooting with AI assistance",
  "scripts": {
    "lint": "node scripts/lint.mjs",
    "test": "node --test",
    "test:ci": "node --test --test-reporter=spec",
    "build": "node scripts/build.mjs",
    "verify": "npm run lint && npm run test:ci && npm run build"
  }
}
